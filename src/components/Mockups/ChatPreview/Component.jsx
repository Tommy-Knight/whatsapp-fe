import './style.css'

const ChatPreview = () => {
    return (
        <div className={}>
            <span> {'< icon >'} </span>
            <span> {' < Name >'} </span>
            <span> {' < partial last msg >'} </span>
        </div>
    )
}

export default ChatPreview